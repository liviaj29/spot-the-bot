<div class="browser-container">
  <div class="browser-header">
    <button class="browser-btn">⬅</button>
    <button class="browser-btn">🔄</button>
    <input type="text" class="url-bar" value="<%= @question.url %>" readonly>
  </div>

  <div class="webpage-view">
    <img src="<%= @question.screenshot_url %>" alt="Website Preview" class="web-screenshot">
  </div>

  <form action="<%= answer_quiz_path(@quiz, category: 'website', index: @index) %>" method="post">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <button type="submit" name="answer" value="true" class="btn btn-danger">Phishing</button>
    <button type="submit" name="answer" value="false" class="btn btn-success">Safe</button>
  </form>
</div>
